server:
  port: 8181

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa?serverTimezone=Asia/Seoul
    username: spring
    password: spring
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    database: mysql
  servlet:
    multipart:
      max-file-size: 10MB # 파일 1개의 최대 사이즈
      max-request-size: 20MB # 전체 요청의 최대 사이즈

logging.level:
  org.hibernate.SQL: debug

# swagger setting
springdoc:
  swagger-ui:
    path: /myapi.html

jwt:
  secret: 0x6mZgamFNFjZ2SOwX0mazaygZYMcL3vjQ3vr6Za6n/gmD49dtNuI6UItld/jfqGzY1keD/TtaiHgMw4PSmgSg==

# 업로드 파일 저장 경로
upload:
  path: C:/todo_upload
